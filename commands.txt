git hub commands for sync
---
git add .
git commit -m "somemessage"
git push origin main 


-- run beofore running a python file  
source /workspaces/data_engineering_projects/.venv/bin/activate


-- Run this command to remove the virtual environment from Git's index without deleting the actual files on your disk:
git rm -r --cached .venv


-- updating commit 

git add .gitignore
git commit --amend --no-edit
git push origin main --force

-- total size 
du -sh .

-- obeject wise size 
du -h -d 1 | sort -h